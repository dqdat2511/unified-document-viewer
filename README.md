# Unified-Document-Viewer
a backend system that provides a single search interface for vehicle documents, aggregating results from two independent external systems (Sales System and Service System) in parallel, identified by Vehicle Identification Number (VIN).
